尽量减少空指针异常（对象直接new；考虑会不会空指针=》传参、类型转换判断......
foreach循环判断元素存在=》繁琐；尝试lambda表达式
错误返回：writeline只适合调试，建议抛异常；给调用者返回一些信息（bool值是否成功、异常、int值多少个......
多个remove 建议从后向前
select返回的是order（的列表）而不是void

order数据类，一般建议set和get
orderdetail和order多对一
查询可以用select where
addorder可以用linq语句
修改订单：新数据覆盖
时间不能用==（太难精确，一般用时间范围
传来的是字符串